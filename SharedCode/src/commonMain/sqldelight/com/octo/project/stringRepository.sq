CREATE TABLE stringList (
  string TEXT NOT NULL
);

-- Further SQL statements are proceeded by an identifier.
selectAll:
SELECT *
FROM stringList;

insertRow:
INSERT INTO stringList(string)
VALUES (?);